# bots
Ours bots, now in a version control system!

### Running Hilda
```sh
$ cd bots/
$ poetry install
$ # Make sure to create a .env file:
$ # .env
$ # BOT_TOKEN=<token>
$ # RUNNING_LOCALLY=<1|0>
$ poetry run python hilda
```
